{
  "name": "home-video",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "concurrently -k \"npm --workspace apps/api run dev\" \"npm --workspace apps/web run dev\"",
    "test": "npm --workspace apps/api test && npm --workspace apps/web test",
    "lint": "npm --workspace apps/api run lint && npm --workspace apps/web run lint",
    "build": "npm --workspace apps/web run build",
    "start": "npm --workspace apps/api run start"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
